<Window x:Class="Watchdog.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Watchdog"
        xmlns:vm="clr-namespace:Watchdog.ViewModels"
        xmlns:conv="clr-namespace:Watchdog.Converters"
        mc:Ignorable="d"
        Title="Watchdog" Height="450" Width="800">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <conv:InverseBooleanConverter x:Key="InverseBoolConverter" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Add New Watchdog Task -->
        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="URL:" Margin="0,0,5,0" VerticalAlignment="Center"/>
            <TextBox Width="200" Text="{Binding NewUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
            <TextBlock Text="Interval (seconds):" Margin="5,0,5,0" VerticalAlignment="Center"/>
            <TextBox Width="50" Text="{Binding NewInterval, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
            <Button Content="Add" Command="{Binding AddWatchdogTaskCommand}" Padding="5"/>
        </StackPanel>

        <!-- List of Watchdog Tasks -->
        <ListView Grid.Row="1" ItemsSource="{Binding WatchdogTasks}" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="URL" Width="200" DisplayMemberBinding="{Binding Url}"/>
                    <GridViewColumn Header="Interval (s)" Width="80" DisplayMemberBinding="{Binding Interval}"/>
                    <GridViewColumn Header="Status" Width="150" DisplayMemberBinding="{Binding Status}"/>
                    <GridViewColumn Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Start" Command="{Binding DataContext.StartWatchdogTaskCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}" Margin="0,0,5,0" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                                    <Button Content="Stop" Command="{Binding DataContext.StopWatchdogTaskCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}" Margin="0,0,5,0" IsEnabled="{Binding IsRunning}"/>
                                    <Button Content="Remove" Command="{Binding DataContext.RemoveWatchdogTaskCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>